const String baseUrl = String.fromEnvironment(
  'BASE_URL',
  defaultValue: 'https://api.gigger.art',
);

const String redirectUrl = String.fromEnvironment(
  'REDIRECT_URL',
  defaultValue: 'https://app.gigger.art',
);
